server:
  port: ${PORT:8080}

spring:
  application:
    name: airconnect
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  web:
    resources:
      static-locations: classpath:/static/,classpath:/public/,classpath:/resources/,classpath:/templates/

  jackson:
    default-property-inclusion: always
    serialization:
      write-dates-as-timestamps: false
      write-null-map-values: true
    time-zone: Asia/Seoul

  datasource:
    url: jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQLDialect

  data:
    redis:
      url: ${REDIS_URL}

  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.hibernate.SQL: INFO
    org.hibernate.orm.jdbc.bind: INFO
    univ.airconnect: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

kakao:
  base-url: https://kapi.kakao.com
  me-path: /v2/user/me

jwt:
  secret: ${JWT_SECRET}
  access-token-expiration-seconds: ${JWT_ACCESS_TOKEN_EXPIRATION_SECONDS:3600}
  refresh-token-expiration-seconds: ${JWT_REFRESH_TOKEN_EXPIRATION_SECONDS:2592000}

iap:
  enabled: ${IAP_ENABLED:true}
  apple:
    bundle-id: ${IAP_APPLE_BUNDLE_ID:${APPLE_CLIENT_ID:${BUNDLE_ID:com.airconnect.app}}}
    environment: ${IAP_APPLE_ENVIRONMENT:SANDBOX}
    allowed-product-ids: ${IAP_APPLE_ALLOWED_PRODUCT_IDS:}
  google:
    package-name: ${IAP_GOOGLE_PACKAGE_NAME:com.airconnect.app}
    service-account-json-path: ${IAP_GOOGLE_SERVICE_ACCOUNT_JSON_PATH:}
    verify-enabled: ${IAP_GOOGLE_VERIFY_ENABLED:false}

apple:
  team-id: ${APPLE_TEAM_ID:${TEAM_ID}}
  key-id: ${APPLE_KEY_ID:${KEY_ID}}
  client-id: ${APPLE_CLIENT_ID:${BUNDLE_ID}}
  service-id: ${APPLE_SERVICE_ID:}
  private-key-path: ${APPLE_PRIVATE_KEY_PATH:${KEY_PATH}}
  # backward compatibility for legacy key names
  bundle-id: ${BUNDLE_ID}
  key-path: ${KEY_PATH}
  login:
    issuer: ${APPLE_LOGIN_ISSUER:https://appleid.apple.com}
    jwks-url: ${APPLE_LOGIN_JWKS_URL:https://appleid.apple.com/auth/keys}
    allowed-audiences: ${APPLE_LOGIN_ALLOWED_AUDIENCES:}
  revoke:
    enabled: ${APPLE_REVOKE_ENABLED:true}
    client-id: ${APPLE_REVOKE_CLIENT_ID:${APPLE_CLIENT_ID:${BUNDLE_ID}}}
    endpoint: ${APPLE_REVOKE_ENDPOINT:https://appleid.apple.com/auth/revoke}
    client-secret-ttl-seconds: ${APPLE_CLIENT_SECRET_TTL_SECONDS:300}

moderation:
  report:
    duplicate-window-minutes: ${MODERATION_REPORT_DUPLICATE_WINDOW_MINUTES:60}
  support:
    email: ${SUPPORT_EMAIL:}
    url: ${SUPPORT_URL:}
    contact-text: ${SUPPORT_CONTACT_TEXT:}

app:
  upload:
    profile-image-dir: ${PROFILE_IMAGE_DIR:/var/lib/airconnect/profile-images}
    profile-image-url-base: ${PROFILE_IMAGE_URL_BASE}
  websocket:
    inbound:
      core-pool-size: ${WS_INBOUND_CORE_POOL_SIZE:16}
      max-pool-size: ${WS_INBOUND_MAX_POOL_SIZE:64}
      queue-capacity: ${WS_INBOUND_QUEUE_CAPACITY:5000}
      keep-alive-seconds: ${WS_INBOUND_KEEP_ALIVE_SECONDS:60}
    outbound:
      core-pool-size: ${WS_OUTBOUND_CORE_POOL_SIZE:16}
      max-pool-size: ${WS_OUTBOUND_MAX_POOL_SIZE:64}
      queue-capacity: ${WS_OUTBOUND_QUEUE_CAPACITY:5000}
      keep-alive-seconds: ${WS_OUTBOUND_KEEP_ALIVE_SECONDS:60}
    transport:
      send-time-limit-ms: ${WS_SEND_TIME_LIMIT_MS:15000}
      send-buffer-size-limit-bytes: ${WS_SEND_BUFFER_SIZE_LIMIT_BYTES:1048576}
      message-size-limit-bytes: ${WS_MESSAGE_SIZE_LIMIT_BYTES:131072}

notification:
  outbox:
    worker:
      enabled: ${NOTIFICATION_OUTBOX_WORKER_ENABLED:true}
      delay-ms: ${NOTIFICATION_OUTBOX_WORKER_DELAY_MS:1000}
      recovery-delay-ms: ${NOTIFICATION_OUTBOX_WORKER_RECOVERY_DELAY_MS:60000}
  push:
    fcm:
      enabled: ${NOTIFICATION_PUSH_FCM_ENABLED:true}
      app-name: ${FIREBASE_APP_NAME:airconnect-notification}
      project-id: ${FIREBASE_PROJECT_ID:}
      credentials-path: ${FIREBASE_CREDENTIALS_PATH:}
