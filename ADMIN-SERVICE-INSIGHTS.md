# 서비스 현황판 구현 / 검증 기록 (2026-09-12)

기존 `/docs/` Git 제외 규칙을 변경하지 않기 위해 이 보고서는 저장소 루트에 둔다.

## 범위

기존 운영 홈은 `운영 점검`에 보존하고 기본 탭에 `서비스 현황`을 추가했다.
백엔드 기존 데이터 읽기 전용 집계와 관리자 프론트만 변경했다. 운영 서버/DB/Redis 접근,
계정 조치, 결제, 티켓 조정, 공지 발송, 커밋, 푸시, 배포를 실행하지 않았다.
기존 알림 관련 미커밋 변경과 staged 엑셀 파일은 이번 작업에 포함하지 않았다.

## 화면

- 핵심 숫자: 현재 가입 완료, 신규 계정, 활동 이벤트 회원, 1:1 수락, 메시지 작성 회원, 탈퇴 기록.
- 날짜별 가입/탈퇴/가입 완료 이벤트/활동 이벤트 추이. 동일 길이 이전 기간 비교.
- 현재 학과/성별 분포, 사진/학교 인증 네 그룹, 학과/성별/그룹 결합 회원 조회.
- 동일 기간 생성된 1:1 요청의 현재 결과와 대화 전환. 시스템 메시지·삭제 메시지 제외,
  실제 양쪽 매칭 당사자의 TEXT/IMAGE 전송만 서로 대화로 인정.
- 그룹 2:2/3:3 남녀 DB 대기 팀, 단계, 현재 평균 대기, 기간 내 최종방 생성 기록.
- 현재 최근 1/7/30일 마지막 활동 기록, 장기 활동 기록 없음 회원.
- D1/D7 관찰 완료 대상별 재방문 표본, 가입 주차별 비교, 요일/시간대 히트맵.
- 세션 시작 이벤트, 운영 주문 상태별 주문/고유 회원 수, 티켓 출처별 지급/차감.
- 관련 회원 상세 및 기존 매칭/그룹/채팅/티켓 화면으로 이동. 관련 복귀는 기존 메모리 navigation 사용.
- 1366×768 노트북 크기를 기준으로 작은 KPI와 두 열 그래프. #ffe300 / Pretendard 유지.

## API 계약 (추가만, 기존 API 변경 없음)

`GET /api/v1/admin/insights?from=YYYY-MM-DD&to=YYYY-MM-DD`

`GET /api/v1/admin/insights/members?from=...&to=...&segment=current&department=...&gender=MALE&page=0`

- 기존 `/api/v1/admin/**` ADMIN 인증 경계 및 관리자 API 접근 감사 적용. 응답 `Cache-Control: no-store`.
- 기존 ApiResponse envelope. overview 데이터 키: from/to/generated_at/timezone/previous_from/previous_to/
  partial_day/snapshot/period/previous/daily/departments/readiness/genders/matching/groups/commerce/activity/notes.
- members: items/total/page/size. 고정 25개, 안정적 created_at DESC,id DESC 정렬.
- segment: current/incomplete/inactive/signups/withdrawals/active/chat/both/photo/email/neither.
- gender: MALE/FEMALE/UNKNOWN. department는 정확한 값, 미등록은 `미입력`.
- 날짜는 한국 시간 달력 기준 양끝 포함, SQL 타임스탬프는 시작 이상/다음날 미만.
  오늘 종료는 조회 시각까지 제한한다. 미래·역순·90일 초과는 INVALID_REQUEST.
- 가입/매칭 등 기존 LocalDateTime은 JVM 저장 시간대 관례, 채팅은 코드상 UTC 저장을 별도 적용.
  과거 서버 시간대를 변경했다면 데이터 시간대 정합성은 별도 확인 필요.
- read-only repeatable-read transaction, 30초 timeout. 새 테이블·마이그레이션 없음.
- 관리자 제외. `admin.insights.excluded-user-ids`(쉼표 구분 ID)로 명시된 테스트 회원만 추가 제외.
  임의로 이름·이메일을 보고 테스트 회원이라 판단하지 않는다. 현재 설정값/운영 ENV는 읽지 않았다.
- 그룹 계정 제외는 팀장 기준임을 UI에 표시. IAP 주문은 PRODUCTION만.
- 집계 SQL은 본문·사진 파일·이메일 문자열·토큰·영수증·기기 ID를 선택하지 않는다.
  회원 목록은 id/이름/닉네임/학과/성별/상태/사진인증 여부/가입일만 반환한다.

## 지표 해석과 아직 미수집인 부분

- 현재 가입 완료 분모 = 탈퇴가 아닌 FULL 회원. 정지/제한 회원은 별도 탈퇴로 취급하지 않는다.
- 스냅샷은 조회 시점 기준. 기간 선택으로 과거 사진/인증/학과 상태를 재구성하지 않는다.
- 탈퇴 집계는 현재 남아 있는 DELETED 및 deleted_at 기록이다. 복구 및 영구 삭제 이전
  탈퇴를 복원하지 않는다. 그래서 엄밀한 누적 탈퇴/순증 지표로 부르지 않는다.
- 가입 완료/활동 이벤트는 수집 표본이다. 활동 0건이면 상단에 수집 확인 필요를 표시한다.
  최근 last_active_at 집계는 서버 요청 기록이지 앱 전면 사용 또는 실시간 접속자가 아니다.
- 히트맵은 날짜별 고유 회원 수의 합(인·일), 기간 전체 고유 인원으로 해석하지 않는다.
- D1/D7은 가입 완료가 아닌 계정 생성일 기준, 해당 한국 달력 날짜가 끝난 회원만 분모.
  이력 누락/탈퇴 익명화/재가입은 남아 있는 기록 한계가 있다.
- 같은 기간 생성 요청의 상태는 현재 상태이며 과거 스냅샷이 아니다. 대화 기록은 선택 기간
  종료까지로 제한한다. UI에 두 기준을 명시했다. 연결 방 수/메시지 수는 실제 만남/만족도가 아니다.
- 평균·중앙 전면 이용 시간은 `NOT_COLLECTED`. 세션 시작/종료 간격을 사용 시간으로 추정하지 않는다.
  **남은 연동**: 실제 모바일 앱의 전면/백그라운드, 중복 방지 이벤트 ID, 세션 ID,
  활성 시간 누적 계약 및 전송 검증. 이번 저장소 범위에 모바일 앱 구현은 포함되지 않는다.
- 매출 금액은 `NOT_COLLECTED`: 검증된 주문 금액/통화 계약 확인 전 임의 가격 환산 없음.
- 세션 시작 수는 중복 전송이 확인되지 않은 이벤트 수이며 실제 방문 횟수가 아니다.
- 주문 상태는 기간 내 생성 주문의 현재 상태. 환불 발생일별 매출 보고서가 아니다.
- 과거 학과별 탈퇴 분석, 엄밀한 순증, 정확한 만료 횟수는 불변 이력이 부족하여 만들지 않았다.

## 검증

- 새 집계 SQL을 독립 H2 MySQL 호환 모드에서 실행: 현재 분모/네 그룹, 제외 계정,
  결합 필터/SQL 문자열 안전성, 한국 날짜 경계, 고유 활동 인원, 양측 실제 대화,
  삭제·시스템·제삼자 메시지 배제, sandbox 구매 제외, 범위 거절, 업무 데이터 비변경.
- MockMvc + ADMIN 역할 필터: 익명 401, 일반 회원 403, 관리자 응답/캐시 금지/잘못된 날짜.
- 기존 AdminOperationsService 회귀 테스트 포함. 결과는 test report로 확인.
- 최종 백엔드 15개 통과(신규 10 + 기존 운영 집계 5), 프론트 150개 통과.
  TypeScript/변경 파일 lint/build 통과. git diff --check 및 새 파일 no-index check 오류 없음.
  프론트 추적 파일 diff stat은 2 files, +263/-1이며 신규 5개 파일은 별도 미추적이다.
  백엔드는 신규 소스 3개/테스트 2개/문서 1개이며 기존 알림 변경 diff는 본 작업과 무관하다.
- 프론트 TypeScript 검사, 새 지표 유틸/메모리 복귀 테스트 및 기존 테스트 실행.
- 테스트 전용 `tests/fixtures/insights-browser.html`은 운영 엔트리에서 import하지 않는다.
  고정 fake client만 사용하며 운영 API 호출·실제 로그인·변경 작업은 할 수 없다.
- 가상 브라우저: 7일 → 회원 2페이지 → 가상 상세 → 복귀 시 날짜/페이지 유지,
  매칭/대기/활동/구매 화면 표시, 1366×768 화면 확인, 1024×768 가로 넘침 없음 확인.
  실제 회원 상세 UI 자체는 stub이므로 이번 브라우저 검증 범위가 아니다.
- 운영 Firebase나 실제 backend 통합은 검증하지 않았다. Docker CLI/로컬 MySQL이
  확인되지 않아 실제 MySQL 엔진 실행·실데이터 성능·실행 계획 검증은 미실행.
- 필요 시 MySQL EXPLAIN 후 기존 인덱스를 확인하여 users(created_at), users(deleted_at),
  chat_messages(room_id,sender_id,created_at), analytics_events(user_id,occurred_at,type)를 검토한다.
  운영 인덱스 추가는 이번에 실행하지 않았다.

## 지원 범위와 인계

브라우저 뒤로/앞으로 및 관련 화면 복귀는 동일 탭 메모리에서 기간/필터/페이지 유지.
새로고침은 기존 navigation 정책대로 기본 홈/최근30일로 복귀하며 검색 조건은 영구 저장하지 않는다.
새 API 배포 전 프론트만 배포하면 조회 실패가 발생한다. 백엔드 검증/배포 후 프론트 배포 순서.
검증된 로컬 구현과 실제 운영 검증은 분리해서 판단해야 한다.

최종 운영 투입 판정: NEEDS_ADJUSTMENT — 실제 MySQL 통합/성능, 모바일 활동 수집 연동,
금액·탈퇴 불변 이력은 남아 있다. 화면의 집계 준비 중 항목을 완료된 분석으로 보고하지 않는다.
